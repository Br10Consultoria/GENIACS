# A interface web do GenieACS está incluída na mesma imagem construída por ../genieacs/Dockerfile.
# Este arquivo permanece apenas por compatibilidade com a estrutura antiga do repositório.
# Use os arquivos docker-compose.*.yml, que apontam para o Dockerfile único corrigido.
FROM geniacs-genieacs:latest
CMD ["genieacs-ui"]
